const INITIAL_STATE = {
  isLoading: false,
  trending: [],
  searchResults: []
};

const shopReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default shopReducer;
